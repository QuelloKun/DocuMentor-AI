# DocuMentor AI - Phase 2: Local Fine-tuning Requirements
# Optimized for RTX 3070 (8GB VRAM)

# Core ML Training Framework
torch>=2.1.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.24.0

# Memory-Efficient Training
peft>=0.7.0                    # LoRA and QLoRA
bitsandbytes>=0.41.0          # 4-bit quantization
trl>=0.7.0                    # Transformer Reinforcement Learning

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Progress Tracking & Logging
wandb>=0.16.0                 # Weights & Biases for experiment tracking
tqdm>=4.65.0

# Model Serving & Inference
gradio>=4.0.0                 # Web interface for testing
flask>=3.0.0                  # REST API

# Development Tools
jupyter>=1.0.0
ipywidgets>=8.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: Advanced features
flash-attn>=2.3.0             # Flash Attention (if compatible)
einops>=0.7.0                 # Tensor operations 